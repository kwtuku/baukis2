<header>
  <%= link_to "BAUKIS2", :staff_root, class: "logo-mark" %>
  <%= tag.span(flash.notice, class: "notice") if flash.notice %>
  <%= tag.span(flash.alert, class: "alert") if flash.alert %>
  <%=
    if defined?(current_staff_member) && current_staff_member
      link_to "ログアウト", :staff_session, data: { turbo_method: :delete }
    else
      link_to "ログイン", :staff_login
    end
  %>
  <%= link_to "アカウント", :staff_account if defined?(current_staff_member) && current_staff_member %>
</header>
